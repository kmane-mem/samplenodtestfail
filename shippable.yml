# Language setting
language: node_js

# Version number
node_js:
  - 8.1

#env:
# - test="latest"
# - test="masterryt"

git:
   submodules: false
   
build:
  #pre_ci:
    #- docker build -t=shippabledocker/hitchcock3_sample_nodetest .
   

  pre_ci_boot:
    image_name: drydock/u16nodtall
    image_tag: master
    pull: true
    options: "-m=8m"

  ci:
    - service --status-all

  #post_ci:     
    #- docker push shippabledocker/hitchcock3_sample_nodetest
    
integrations:
  hub:
 #Docker integration
    - integrationName: ship-docker
      type: docker
