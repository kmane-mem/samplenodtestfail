language: node_js

# Version number
node_js:
  - 8.2

build:
  pre_ci:
    - jfrog rt u testpreci.sh sample_app
    #- jfrog rt dl test-repository-local/codeblocks.tar.xz
    
    
 # pre_ci_boot:
   # image_name: drydock/u14nodall
   # image_tag: master
   # pull: false
  #  options: '--privileged=false --net=bridge -e FOO=true -e BOO=false'

  ci:
    - echo "hello world!!!"
    - jfrog rt u testci.sh sample_app
  #  - jfrog rt dl test-repository-local/codeblocks.tar.xz

integrations:
  hub:
    - integrationName: ship-jfrog
      type: artifactory
