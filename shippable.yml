language: node_js

# Version number
node_js:
  - 8.2

build:
  pre_ci:
    - jfrog rt u uploadpreci.sh sample_app
    - jfrog rt dl sample_app/testpreci.sh   
    
 # pre_ci_boot:
   # image_name: drydock/u14nodall
   # image_tag: master
   # pull: false
  #  options: '--privileged=false --net=bridge -e FOO=true -e BOO=false'

  ci:
    - echo "hello world!!!"
    - jfrog rt u uploadci.sh sample_app
    - jfrog rt dl sample_app/testci.sh

integrations:
  hub:
    - integrationName: ship-jfrog
      type: artifactory
